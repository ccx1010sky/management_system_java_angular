version: 0.2
phases:
  pre_build:
    commands:
      - cd springboot_backend
  build:
    commands:
      - echo Entered the build phase....
      - mvn clean package
  post_build:
    commands:
      - echo Build completed successfully
artifacts:
  files:
    - target/srpingboot_backend.jar
  discard-paths: yes
